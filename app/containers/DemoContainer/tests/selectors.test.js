// import { selectDemoContainerDomain } from '../selectors';

describe('selectDemoContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
